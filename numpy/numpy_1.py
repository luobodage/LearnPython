import numpy as np 

# a = np.array([[1,2],[3,4]]) #创建数组
# a = np.arange(16).reshape(4,-1) #生成4×4矩阵
# a = np.zeros([4,4],dtype = np.int16) #定义为整型数据 全零
# a = np.ones([4,4],dtype = np.int16) #全一
# a = np.eye(4) # 4×4矩阵 中间都为1
#  #随机数组 18个数据 2×8
# a = np.random.randint(1,100,16).reshape(2,8) # 随机生成整数型变量
# a = np.random.randint(1,100,size = (2,2)) #另一种写法
# a = np.linspace(1,10,10) #等差数列
# a = np.logspace(1,2,10) #等比数列


a = np.arange(9)
print(a)
